<h1>Manage Collaborators for <%= @wiki.title %></h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Username</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <% @users.each do |user| %>
        <tr>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td>
                <% if @wiki.users.include?(user) %>
                    <% collaborator = user.collaborator_for(@wiki) %>
                    <%= link_to "Remove as collaborator", wiki_collaborator_path(@wiki, collaborator), method: :delete, class: "btn btn-danger" %>
                <% else %>
                    <%= link_to "Add as Collaborator", wiki_collaborators_path(@wiki, user_id: user.id), method: :post, class: "btn btn-success" %>
                <% end %>
            </td>
        </tr>
    <% end %>
</table>